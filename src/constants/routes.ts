type Routes = {
  HOME: string;
  CREATE_ESTIMATE: string;
  ESTIMATES_HISTORY: string;
  ESTIMATES_FULL_INFO: string;
};

export const ROUTES: Routes = {
  HOME: '/',
  CREATE_ESTIMATE: '/create-estimate',
  ESTIMATES_HISTORY: '/estimates-history',
  ESTIMATES_FULL_INFO:'/estimates-history/estimate-full-info'
};
